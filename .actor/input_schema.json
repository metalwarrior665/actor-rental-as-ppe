{
    "title": "CheerioCrawler Template",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "requestsCount": {
            "title": "Requests Count",
            "type": "integer",
            "default": 50,
            "description": "Number of requests to simulate in this run."
        },
       "prepaidRentalResultsCount": {
           "title": "Prepaid Rental Results Count",
           "type": "integer",
           "default": 100,
           "description": "Number of simulated prepaid rental results. This would normally be set in the code but is included here for demonstration purposes."
       }
    }
}
